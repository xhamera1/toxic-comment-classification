# Python 3.11 minimal stack (Windows-friendly)
# Essential libraries for this project without heavy/problematic installs.

# --- Notebook & environment (Cursor / VS Code) ---
# Avoid `jupyter` metapackage on Windows: it pulls JupyterLab + very long paths under
# `.venv/share/jupyter/labextensions/...` and fails if "Long paths" are disabled.
# For running *.ipynb inside Cursor, ipykernel + ipython is enough.
ipykernel>=6.29.0
ipython>=8.12.0
# Optional: classic Notebook in the browser only if you need it:
# notebook>=7.0.0

# --- Data & EDA ---
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
matplotlib>=3.7.0
seaborn>=0.13.0

# --- Classical ML / metrics / splits ---
scikit-learn>=1.3.0

# --- Hugging Face / Transformer workflow ---
torch>=2.1.0
transformers>=4.36.0,<5.0.0
datasets>=2.16.0
accelerate>=0.26.0
evaluate>=0.4.0
safetensors>=0.4.0

# --- Utilities ---
tqdm>=4.66.0

# Optional extras:
# optuna>=3.5.0
# tensorflow>=2.15.0,<2.19.0  # keep optional due to Windows long-path install issues
